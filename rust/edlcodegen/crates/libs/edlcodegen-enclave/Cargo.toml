[package]
name = "edlcodegen-enclave"
version = "0.1.0"
edition =  { workspace = true }
license = "MIT"
keywords = ["vbs", "vbs-enclave", "vbs-enclave-codegen", "enclave"]
repository = "https://github.com/microsoft/VbsEnclaveTooling"
categories = ["os", "no-std"]
description = "Enclave-side ABI and runtime support for Rust crates generated by edlcodegen"

[dependencies]
edlcodegen-core = { workspace = true }
spin = { workspace = true }
windows-link = { workspace = true }

[features]
mock_functions = []